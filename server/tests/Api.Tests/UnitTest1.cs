namespace Api.Tests;

// This placeholder test keeps the API test project wired into dotnet test.
// Replace it with controller or endpoint tests as the API behaviour grows.
public class UnitTest1
{
    [Fact]
    public void Test1()
    {
        // No assertion yet: the value today is proving the test project compiles and runs.
    }
}
